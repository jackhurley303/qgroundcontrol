# macOS Plugin SDK — feature-change tracker

**Profile:** `/feature-change` (additive) for Stage 1; Stages 2–3 switch to `/architecture-change` per the blueprint's Execution notes (§11 of the plan doc).
**Blueprint:** [plugins/.architecture/04-macos-implementation-plan.md](../.architecture/04-macos-implementation-plan.md) — grounded design, locked decisions D1–D11, spikes S1/S2m/S3/S4/S6m all PASS (2026-07-06).

## Status

### Stage 1 — The declared contract (manifest + loader gate)

- **U1.1 — `PluginManifest` value type + validation + tests** — DONE (2026-07-09).
  - New: `src/PluginSystem/PluginManifest.h/.cc` — value type (`id, name, version, vendor, description, tier, apiVersion, hostVersionMin/Max, hostBuildId, contributes`), `fromJson()`, `validateForHost()`.
  - Wired into `src/PluginSystem/CMakeLists.txt`.
  - Tests: `test/PluginSystem/PluginManifestTest.{h,cc}` (11 cases) registered in `test/CMakeLists.txt` + `test/PluginSystem/CMakeLists.txt`. All pass (`--unittest:PluginManifestTest`).
  - Pure logic, no libraries loaded — matches the plan's test description exactly.
  - Next: U1.2 (loader gate: metadata before code) in a fresh chat.

- **U1.2 — Loader gate: metadata before code** — DONE (2026-07-09).
  - `QGCPluginLoader::_loadPlugin` split into `_inspect(filePath)` (metaData → IID → `PluginManifest::fromMetaData` → `validateForHost`, zero code execution) + `_activate(info)` (instance/cast/createPlugin). `PluginLoadInfo` grew `manifest`/`state` (full `PluginState` enum incl. not-yet-used `Disabled`/`Quarantined`)/`errorString`; loader stores all inspected results, `loadedPluginInfos()` stays Active-only so `QGCPluginManager` is untouched; new `knownPluginInfos()` exposes everything for U1.3/U1.4.
  - `PluginManifest::fromMetaData(envelope, expectedIid)` added (pure, tested); host identity via `QGCPluginLoader::hostInfo()` from `QGC_APP_VERSION_STR` + new `QGC_GIT_HASH` in `qgc_version.h.in`; `inline constexpr QGCPluginApiVersion = 1` in `QGCPluginInterface.h` (U2.1 bumps this one place).
  - Both plugins stamp `Q_PLUGIN_METADATA(IID QGCPluginInterface_iid FILE "qgcplugin.json")` with `qgcplugin.json.in` configured per plugin (example: `org.qgroundcontrol.example`; qdrive: `org.qdrive.plugin` — ids still cheap to change until U1.3 keys settings by them). QDrive changes are a separate commit in its nested repo.
  - Verified: 18 PluginManifestTest cases green; live boot logs "Validated <name> (internal, build <hash>) before load" for both plugins; wrong-IID dylib rejected legibly without crash. `/code-review` medium: 8 findings, 6 fixed (incl. null-host-version rejecting all plugins on tagless checkouts — range check now skipped when host version unparseable), 2 skipped (git-less "0000000" build-id sentinel match; inspect→activate TOCTOU — closed later by U3.3's file-hash consent).

- **U1.3 — Manager: id-keyed records, no execution of disabled plugins** — DONE (2026-07-10).
  - `QGCPluginManager` now keeps `QList<PluginLoadInfo> _records` (one per discovered plugin, any state) — the plan's `PluginRecord` realized by reusing U1.2's `PluginLoadInfo` rather than a parallel struct. Flow: `inspectDirectories` → `_processInspected` (register id with settings → duplicate-id guard → skip disabled **without ever calling `instance()`** → `_activateRecord`). The 01 §3.5 defect (disabled plugins executing code) is dead.
  - `QGCPluginLoader` became a fully **static, stateless** inspect/activate utility (no QObject, no `_pluginInfos`, `loadPlugins`/`loadedPluginInfos`/`knownPluginInfos`/signals deleted; directory scan sorted by name for deterministic duplicate resolution).
  - `PluginSettings` keys Facts by manifest id (D5, no shim): `registerPlugin(id, displayName, defaultEnabled)`, `registeredPluginIds`. Interim default rule (Example off by id `org.qgroundcontrol.example`, rest on) lives in the manager, awaiting D10/U3.3.
  - API: `unloadPlugin`/`reloadPlugin(name)` → `setPluginEnabled(id, bool)` (writes Fact + reconciles activation, idempotent/reentrancy-safe) + `reloadPlugin(id)` (stored path only, scan fallback deleted; refuses id-change-on-disk to keep settings keys coherent). QML page minimally updated (full revamp = U1.4). Contribution maps carry `pluginId` (identity) + `name` (display, from manifest).
  - Tests: `test/PluginSystem/QGCPluginManagerTest` (9 cases; fixtures use nonexistent paths so wrongful activation is detectable as Failed-instead-of-Disabled). Full Unit suite green except 3 pre-existing keychain-timeout tests (environmental). `/code-review` high: 7 findings — 4 fixed (reload id-drift, name-keyed default, unsorted scan, static loader), 3 plausible-skipped (latent record-pointer reentrancy; same-display-name QML panel-key collision (pre-existing, U2.2 reworks consumption); stale Fact label after same-id reload (U1.4 reads names from records)).

- **U1.4 — Settings page shows the contract** — DONE (2026-07-10).
  - `QGCPluginManager` gained `knownPlugins()` (`Q_PROPERTY`, NOTIFY reuses `loadedPluginsChanged` — every `_records` mutation site already emits it) returning every record's id/name/version/vendor/description/state (raw enum name, for QML color-coding)/statusText (composed human line: "Active", "Disabled", "Incompatible: <reason>", "Failed to load: <reason>", "Quarantined: <reason>").
  - `PluginSettings.qml` Repeater now iterates `knownPlugins` instead of `registeredPluginIds`: each row shows name/version/vendor (read from the record, not `fact.label`) + the enable toggle + a colored status line beneath (green/red/default by state). Absorbed both U1.3 review leftovers: display name no longer goes stale after a reload, and Incompatible/Failed rows now show why they aren't running instead of just an ambiguous toggle. Empty-state check switched from `loadedPlugins` to `knownPlugins`.
  - Tests: `_knownPluginsReflectsRecords_test` added to `QGCPluginManagerTest` (Active + Incompatible fixtures, verifies map shape and composed statusText). Full suite green except the 3 pre-existing keychain-timeout tests (environmental, same as U1.3). `/code-review low`: no findings.
  - Next: U1.5 (loader hygiene + docs truth) in a fresh chat.

- **U1.5 — Loader hygiene + docs truth** — DONE (2026-07-10).
  - `QGCPluginManager::_activateRecord` ([QGCPluginManager.cc](../../src/PluginSystem/QGCPluginManager.cc)): second plugin to report a replay extension now logs `qCWarning(QGCPluginManagerLog)` naming the plugin id and states first-registration-wins; behavior unchanged (still first wins), just no longer silent.
  - `plugins/README.md` and `src/PluginSystem/README.md` fully rewritten — both were describing a submodule/git-distributed, name-keyed, `pluginInterfaceVersion()`-checked plugin model that predated U1.1–U1.4 entirely. Now describe: manifest schema + fields actually validated, two-phase inspect/activate, id-keyed `PluginSettings`, real search paths (from `QGCPluginLoader::defaultPluginPaths()`), real linkage (`-undefined dynamic_lookup`/`-Wl,--allow-shlib-undefined`, no `qgc_add_plugin()` yet — that's U1.6), and a tier table framed as roadmap (`internal` is the only tier that works today; `sdk`/`qml` are Stage 2/3 targets, not present features).
  - Also removed a stray gitignored `plugins/example/build/` directory (leftover manual build, not part of the commit either way — `plugins/.gitignore` already excludes `build/`).
  - No tests (docs + one `qCWarning`, per plan). `/code-review low`: no findings.
  - Next: U1.6 (`qgc_add_plugin()` becomes the single path) in a fresh chat.

- **U1.6 — `qgc_add_plugin()` becomes the single path** — DONE (2026-07-10). **Stage 1 complete.**
  - `cmake/modules/PluginHelpers.cmake` rewritten: `qgc_add_plugin()` now absorbs manifest plumbing (`MANIFEST` arg → `configure_file` into the binary dir + include path, previously hand-rolled per plugin), `TIER` validation (only `INTERNAL` accepted; `SDK`/`QML` `FATAL_ERROR` pointing at Stage 2/3 — D7's "errors helpfully" framing pulled forward), the undefined-symbol link options (`-undefined dynamic_lookup` / `-Wl,--allow-shlib-undefined`), and the dev-loop auto-deploy `POST_BUILD` step — all previously duplicated verbatim in both plugins' CMakeLists.
  - **Deploy dir is now computed, not hardcoded**: mirrors `QGCApplication.cc`'s `_setInstanceInfo()` applicationName logic exactly (`"${QGC_APP_NAME} Daily"` unless `QGC_STABLE_BUILD`, using `QGC_ORG_NAME`) instead of the literal string `"QGroundControl Daily"` that was in both plugins' CMakeLists before. Correct for any `QGC_APP_NAME`/`QGC_ORG_NAME`/`QGC_STABLE_BUILD` combination now, not just the default.
  - `plugins/example/CMakeLists.txt` and `plugins/qdrive/CMakeLists.txt` (nested repo, separate commit there) both collapse to a `qgc_add_plugin(...)` call; QDrive keeps its extra `target_link_libraries(... Qt6::Sql Qt6::Gui)` after the call (helper doesn't guess a plugin's extra Qt modules — by design, extras stay caller-side per the header doc).
  - `plugins/README.md` "Creating a New Plugin" section updated with the real `qgc_add_plugin()` signature and usage.
  - `plugins/example/build.sh`/`build.bat` (manual single-target rebuild+deploy scripts) left as-is — still correct since the computed deploy dir is unchanged for the default dev config they target.
  - No tests (pure CMake consolidation, no new logic, per plan). Verified: clean build of both plugin targets + full app; both `.dylib`s landed in the build output and were auto-deployed to the live runtime plugin dir; live app run confirmed QDrive plugin initialized and ran normally (log lines, no load errors); manifest content correct in both configured `qgcplugin.json` outputs. `/code-review low`: no findings.
  - Next: Stage 2, U2.1 (library skeleton + type moves) — first unit of the SDK boundary. Entered as an **`/architecture-change`** per the plan's Execution notes (§11): Stages 2–3 are a clean-cutover profile (linkage model reshapes, old model deleted), not additive. Fresh chat.

### Stage 2 — The SDK boundary (`QGCPluginAPI`) — entered as `/architecture-change`

- **U2.1 — Library skeleton + type moves** — DONE (2026-07-10).
  - New `qt_add_library(QGCPluginAPI SHARED)` target in `src/PluginAPI/` (D2/D3): `qgc_plugin_api_global.h` (`QGCPLUGINAPI_EXPORT`), moved `QGCPluginInterface.h` / `QGCPlugin.h/.cc` / `QGCReplayExtension.h/.cc` (verbatim + vtable-freeze warning + export macro), new abstract `QGCHostServices` (`service(id)`, `.cc` metaobject anchor). Hidden visibility + export macro → `nm -gU` shows exactly the API (37 symbols); `VERSION 2.0 SOVERSION 2` → install name `@rpath/libQGCPluginAPI.2.dylib` (matches S2m); PUBLIC include dir on the target is D2's include-isolation enforcement.
  - `QGCPlugin`: gained empty d-pointer (`QGCPluginPrivate`, the S3-validated ABI headroom) and `init(QGCHostServices*)` (default no-op; **null host allowed by the documented contract** — manager passes `nullptr` until the service layer exists, U2.3). The 12 contribution virtuals stay verbatim; U2.2 deletes them. Dropped: inert `QML_UNCREATABLE` (never registered — no `QML_ELEMENT`; verified in `qgroundcontrol_qmltyperegistrations.cpp`) and the never-used `QGCPluginLog` category.
  - **IID/apiVersion single-owner**: `QGC_PLUGIN_API_VERSION_MAJOR` in `QGCPluginInterface.h` derives BOTH the IID string (`org.qgroundcontrol.QGCPluginAPI/2.0`, via stringify macro — moc handles it, verified in the built dylibs' embedded metadata) and `QGCPluginApiVersion = 2` — the D6 drift class (IID vs constant divergence) is now unrepresentable. CMake `VERSION/SOVERSION` and per-plugin manifest `apiVersion` literals remain separate facts (per-dylib compat + per-plugin declaration).
  - Host side: `src/PluginSystem/` kept loader/manager/manifest, links `QGCPluginAPI`; root `QGC_PLUGIN_INCLUDE_DIRECTORIES` gained `src/PluginAPI`. Both plugins: includes → `PluginAPI/…`, `pluginInterfaceVersion()` returns `QGCPluginApiVersion`, manifests `apiVersion: 2` (QDrive = separate commit in its nested repo, gitlink bump rides in the root commit). Docs updated (`plugins/README.md`, both module READMEs, stale `src/API/README.md` version lines).
  - Verified: full build; Unit suite green except the 3 known environmental keychain-timeout tests; `otool -D/-L` + `nm -gU` checks; live offscreen boot — QDrive activated + initialized through the dylib-backed path (internal tier still `dynamic_lookup`, its `QGCPlugin` undefineds resolve from the host-loaded SDK dylib, as S2m predicted). `/code-review high` (8 finder angles): 4 confirmed findings fixed (init-doc promised non-null host while manager passes null; IID/constant two-encodings drift; `<memory>` include order; `Q_UNUSED` semicolon convention), QML-registration regression candidate REFUTED (type was never registered pre-move), rest refuted/deferred-by-design.
  - Next: U2.2 (contributions move to the manifest, D1) in a fresh chat.

- **U2.2 — Contributions move to the manifest (D1)** — DONE (2026-07-11).
  - `QGCPlugin` shrank to `init(host)/cleanup()/replayExtension()` — **13** virtuals deleted (the plan's 12 static-declaration ones + `name()`, which had zero callers; display name already came from the manifest everywhere). New `src/PluginSystem/PluginContributions.h/.cc`: pure `fromManifest(manifest, errorOut)` parses the `contributes` object into the exact legacy `QVariantMap` shapes (keys pinned by whole-map `QCOMPARE` in the new `PluginContributionsTest`, 11 cases). Plan drift found while grounding: schema gained `toolbarSource` (both real plugins set it, `SelectViewDropdown`/`MainWindow` consume it — the plan doc's schema omitted it); the `urlResolver` param deferred to U3.1 (no package path exists yet; URL rule documented instead); icons are bare resource paths, not `qrc:` URLs — reader passes strings through verbatim.
  - `PluginLoadInfo` gained `contributions`, parsed in `QGCPluginLoader::inspect` after `validateForHost` (malformed `contributes` → `Failed` with legible reason; wrong-type top-level `contributes` also rejected in `PluginManifest::fromJson` — a review find). Manager: `_addContributions(record)` publishes the manifest-derived maps (same signals, same timing); `replayExtension()` queried **only when the manifest declares `"replay": true`** (declared-but-null logs a warning); `_recalcLoggingController` keys off `state==Active && contributions.controlsTelemetryLogging` — `MAVLinkProtocol` untouched (it already consumed `hasLoggingController()`). Public `addToolMenuItem` deleted (no external callers).
  - U1.3-review leftover absorbed: QML panel expansion/pop-out state and the panel-geometry `Settings` category rekeyed `name` → `pluginId` (6 QML files); same-display-name collision gone. Accepted one-time cost: previously saved panel geometry orphaned (pre-release, per D5's no-shim stance).
  - Both plugins: overrides deleted, manifests gained real `contributes` blocks (Example: full block incl. panel sizes/positions as template documentation; QDrive: panels+docks, `replay: true`, `telemetryLogging: true` — nested-repo commit). Docs rewritten: `plugins/README.md` schema section, `src/PluginSystem/README.md`, `plugins/example/README.md` toolbar section; stale `src/API/README.md` (taught the deleted virtual pattern, duplicated PluginSystem docs) cut down to a QGCCorePlugin-only page pointing at the canonical docs. Shared `PluginJsonUtils.h` dedupes `requireStringField` (review find).
  - Verified: full build; `PluginManifestTest` 20 / `PluginContributionsTest` 11 / `QGCPluginManagerTest` 14 all green; full Unit suite green except the 3 known environmental keychain-timeout tests; live offscreen boot shows validate-pre-load → Example skipped disabled without code execution → QDrive activated with menu + both panels synthesized from manifest, replay registered, clean shutdown. `/code-review high` (8 angles): 2 confirmed fixed (wrong-type `contributes` silently coerced; `requireStringField` duplication), 1 confirmed-intentional (geometry reset, accepted above), rest refuted (incl. include-order — matches module siblings; data-driven contribution loop — premature at n=3).

- **U2.3 — Host services `qgc.replay/1` + `qgc.telemetryLogging/1`** — DONE (2026-07-11).
  - SDK gained `QGCReplayService.h/.cc` + `QGCTelemetryLoggingService.h/.cc` (pure-virtual `Q_OBJECT`, `.cc` metaobject anchors, `inline constexpr` id constants). Host side: new `src/PluginSystem/HostServices/` — `QGCHostServicesImpl` (id → QObject registry, first-registration-wins) + `QGCReplayServiceImpl` (wraps `LinkManager::startLogReplay`/`LogReplayLink` + the `ParameterManager::registerReplayParamFile`/`Vehicle::registerReplayPlanFile` registries) + `QGCTelemetryLoggingServiceImpl` (delegates to `MAVLinkProtocol`, relays both change signals). Manager: lazy `_ensureHostServices()` on first activation (paths that never activate — disabled sets, manifest-fixture tests — never touch the wrapped singletons); `QGCPlugin::init()` finally receives a **non-null host**.
  - **Plan drift resolved (user-decided):** the plan's "returns the link as `QObject*`" was unimplementable — `LogReplayLink`'s controls are plain public methods, not meta-object invokable. Shipped as a **flat single-session service** (controls on the service; one tlog session at a time — multi-vehicle within it unaffected; concurrent-file playback would be `qgc.replay/2`). The three typed replay signals (`replayMissionUploaded`/`replaySeekMissionResolved`/`replaySeekParamResolved`, payloads `QList<mavlink_mission_item_int_t>`/`ParamSeekValue`) are **excluded from v1 — Q3 design note:** their qdrive consumers are pure host→host plumbing (feed vehicle mission managers / ParameterManager seek APIs) keyed off state the host already owns via the param/plan registries, so the likely Q3 resolution is hoisting that seek-apply logic host-side rather than piping mavlink types through the SDK.
  - Also drift: plan's "registerReplayParamFile(path)" is actually two registries keyed by vehicleId (`ParameterManager::registerReplayParamFile(vehicleId, path)` + `Vehicle::registerReplayPlanFile(vehicleId, path)` = the "plan-registry hook"); telemetry surface includes `hasPendingLog`/`pendingLogName`/`hasPendingLogChanged` (grounded from MAVLinkProtocol + qdrive usage), excludes `autoCompletedLogCount` (QML-only — plugin QML stays on the host QML singleton).
  - `/code-review high` (8 angles; 4 finder agents lost to a session-limit reset, re-run consolidated): **9 confirmed findings, all fixed** — the big ones: SDK doc said `movePlayhead` takes [0.0–1.0] but host expects [0–100] (a contract-following plugin would seek to ~0); `startReplay` swallowed async session failures (active-vehicle refusal / corrupt tlog) leaving `replayActive()` stuck → interface gained `replayError(QString)` + `replayEnded()` and the impl tracks link `disconnected`; sync active-vehicle guard (a rejected dead link's `pause()` would flip LinkManager's global connection-suspend state); `_teardownLink` guard (stopReplay→startReplay same-turn would overlap two links during queued teardown); `setPlaybackSpeed(≤0)` divide-by-zero in the worker; relay disconnect in stopReplay (spurious late `playbackPaused`); braceless ifs vs `.clang-format`; two weak-test cleanups. Refuted: `_link->disconnect()` "disconnects all signals" claim (it's the `LinkInterface::disconnect()` link-close override).
  - Tests: `HostServicesTest` (8 cases: registry lookup/duplicate, default-assembly qobject_cast across the dylib boundary, telemetry delegation reads + signal relay via metaobject-invoked singleton signals, replay inactive no-ops + empty-path rejection + registries smoke). Verified: full build; 4 PluginSystem suites green; full Unit suite green except the 3 known environmental keychain-timeout tests; `nm -gU` shows exactly the two new interfaces exported (30 new symbols); live offscreen boot logs "Registered service qgc.replay/1 / qgc.telemetryLogging/1 → Host services ready" lazily right before QDrive's activation, QDrive runs normally.

- **U2.4 — Host services `qgc.vehicles/1` + `qgc.missions/1` + `qgc.app/1`** — DONE (2026-07-11). **The full D4 service set now exists.**
  - SDK gained `QGCVehicleService.h/.cc` (activeVehicle/vehicles as `QObject*` + add/remove/active-changed signals; meta-object consumption is the documented contract — `property("id")` etc.; lifetime rule: host-owned, hold via QPointer, drop on `vehicleRemoved`), `QGCMissionService.h/.cc` (`missionReady(vehicleId)`, `saveVehicleMissionToFile(vehicleId, filePath)`, `missionReadyChanged(vehicleId, ready)`), `QGCAppService.h/.cc` (app/org name, versionString, savePath, telemetrySavePath, savePathsChanged). Impls in `HostServices/` mirror U2.3's shape; registered in `_ensureHostServices()` (now logs all five ids).
  - **Plan drift resolved:** the plan's `QGCMissionService` method list (`snapshotVehicleIds`/`savePendingLogWithMission`/…) was `TelemetryMissionController`'s *own* QML-facing API, not a host surface — and can't move host-side (it writes qdrive's SQLite repos/settings). The real host dependency is per-vehicle mission snapshot (`PlanMasterController` `startStaticActiveVehicle`+`saveToFile`) + `Vehicle::initialPlanRequestComplete`; that's the shipped surface. `allMissionsReady` stays derived plugin-side (vehicle list + per-vehicle `missionReady`). **Q3 consumes this shape.**
  - Mission impl uses a **transient `PlanMasterController`** (no standing per-vehicle map): saves are gated on `missionReady`, so the cached-items load is synchronous (proven by the MockLink end-to-end test); `syncInProgress()` guard as backstop; offline-editing facts save/restored around the call (review find — `startStaticActiveVehicle` otherwise persists the snapshotted vehicle's firmware/vehicle class into user settings). Vehicle lookup via `MultiVehicleManager::getVehicleById` (review find — no hand-rolled scan).
  - `qgc.app/1` kept minimal per D4: `savePath` is the one grounded qdrive consumer (`QDriveSettings.cc:27`); `telemetrySavePath` added because a plugin must locate the host-written pending tlog (removes qdrive's duplicated `"Telemetry"` constant — the one path that must match the host); the 11 `qgcApp()` sites are parent-object-only, no seam needed.
  - `/code-review high` (8 angles; 5 finder agents lost to a session-limit reset, re-run consolidated): 6 findings — 5 fixed (offline-editing settings mutated by a "read-only" snapshot → save/restore; `.plan`-suffix appended to extensionless paths while returning true → documented in the SDK contract; stale replay plan-file registration shadows the live mission in a snapshot → documented (registry lifecycle is a Q3 consideration); `_vehicleById` duplication; `objectList()` idiom), 1 accepted (transient PMC constructs a full controller graph per save — explicit, infrequent action; avoids standing per-vehicle controllers for every plugin activation).
  - Tests: `HostServicesTest` grew to 13 (default assembly resolves+casts all five ids across the dylib boundary; app-service delegation + savePathsChanged relay; empty-world/unknown-id refusals with no vehicle). New `HostVehicleServicesTest` (7 cases, `VehicleTest`/MockLink base, labels Integration+Vehicle): object handover + `property("id")`, removal/active relay across disconnect, `missionReady` + a real `.plan` snapshot written and valid JSON (the empirical proof of the transient-PMC design), `missionReadyChanged` relay, unknown-id refusals. Full Unit suite green except the 3 known environmental keychain-timeout tests; `nm -gU` shows exactly the three new interfaces (38 symbols); live offscreen boot logs all five ids and QDrive runs normally.
  - **Spike S3 re-run (real header set) — PASS** (2026-07-11, run pre-commit with a temporary churn patch): an out-of-tree pure Tier B plugin (links SDK dylib + Qt only, `@rpath`, no `dynamic_lookup`, manifest tier `"sdk"` apiVersion 2) was frozen, then the host rebuilt with `Vehicle` layout shifted (32 bytes inserted before `_initialPlanRequestComplete` — the member the mission service reads) and a virtual added to `MultiVehicleManager` → the byte-identical frozen plugin loaded and all five services resolved, cast, and answered identically. Also the first in-the-wild proof that the loader gate accepts an `sdk`-tier manifest (U1.1's rules, ahead of U2.5).

- **Pre-U2.5 ABI hardening** — DONE (2026-07-12). Acts on the whole-stage ABI review (`~/.claude/reports/macos-plugin-sdk-abi-review-2026-07-12.md`, Fable 2026-07-12; verdict: contract sound, 10 findings, none landmine-class). Bundled F1+F2+F3+F4+F5(doc)+F7, no behaviour change:
  - **F1** — vtable-freeze `@warning`s added to the three load-bearing classes that lacked them: `QGCPlugin.h` (base class every plugin subclasses), `QGCPluginInterface.h` (its stale "increment the version number" paragraph rewritten around append-only reality: a new major = new IID + new dylib soname + lockstep rebuild, never in-place edits), `QGCHostServices.h` (its single `service()` virtual is frozen; new lookup semantics = new service id).
  - **F2** — dropped the SDK's inert QtQml dependency: removed `QGCReplayExtension.h`'s `#include <QtQmlIntegration>` + `QML_UNCREATABLE` (never registered — no `qt_add_qml_module`) and `Qt6::Qml` from `src/PluginAPI/CMakeLists.txt`. **SDK dylib now links `Qt6::Core` only** (verified `otool -L`: QtQml + transitive QtNetwork gone) — materially smaller Windows/Android SDK surface later.
  - **F3** — anchor discipline: `QGCReplayExtension` ctor/dtor moved out-of-line into its `.cc` (was inline ctor + no dtor — relied on an AUTOMOC accident; it's the only interface consumed across the boundary in both directions); `QGCPlugin`'s three virtual default bodies (`init`/`cleanup`/`replayExtension`) moved to `QGCPlugin.cc` so non-overriding plugins bind the symbol and can pick up evolved defaults (an ABI-safe evolution channel) instead of baking inline copies.
  - **F4** — threading contract written into `QGCHostServices.h`: all services live on the host main thread; call methods and expect signals only there; a threaded service would ship as a new id.
  - **F5 (doc)** — `QGCReplayService.h` now states the service scopes to sessions started through it; host-initiated replay (a user Log Replay comm link) is invisible/unspecified for v1; host-state-aware would be `qgc.replay/2`.
  - **F7** — SOVERSION↔API-major cross-references: `QGC_PLUGIN_API_VERSION_MAJOR` comment ↔ CMake `VERSION/SOVERSION` comment, rule "SOVERSION == API major, bumped together" written in both places.
  - **F10 pulled forward** (review had folded it into U2.5): F2's SDK QtQml removal exposed that both plugins' runtime classes (`ExampleRuntimePlugin`, `QDriveRuntimePlugin`) carried an **inert `QML_ELEMENT`** that only compiled via the SDK's now-removed transitive `<QtQmlIntegration>` include — neither plugin registers these as QML types (`createPlugin()` instantiates them in C++; QDrive's real QML modules are separate STATIC libs). Deleted the dead macro from both. QDrive edit = separate commit in its nested repo.
  - Verified: full app + both plugins build clean; SDK `nm -gU` 108 → 115 (exactly the 7 prescribed anchor additions: QGCReplayExtension ctor + 3 dtor variants + 3 QGCPlugin default bodies, nothing else); 4 PluginSystem unit suites green (incl. HostServicesTest's cross-boundary `qobject_cast`); offscreen boot reaches UI stage with no plugin-load failure (anchors intact — a broken vtable anchor would SIGSEGV at activation per S3's negative control).
  - **Remaining review findings** by owner: **U2.5** = F6 (canonical include spelling `<QGCPluginAPI/…>` — in-tree + out-of-tree must match) + F8 (deliberate signal-name freeze: `currentLogTimeSecs`/`logFileStats` noun-shaped names, `beginStream` vs `play`). **U2.7 / plan §10** = F9 (tier-dependent `apiVersion` validation before any external `qml`-tier manifest) + the Windows/Android platform notes.

- **U2.5 — Example becomes pure Tier B** — DONE (2026-07-12).
  - `cmake/modules/PluginHelpers.cmake`'s `qgc_add_plugin()` gained real `TIER SDK` support alongside `TIER INTERNAL`: SDK tier links only the `QGCPluginAPI` target (Qt comes along transitively via its `PUBLIC Qt6::Core`) — no `QGC_PLUGIN_COMPILE_DEFINITIONS`/`QGC_PLUGIN_INCLUDE_DIRECTORIES`, no mavlink dependency, no undefined-symbol link options; those all moved into the `TIER INTERNAL` branch, unchanged for QDrive. Also gained a `NO_DEPLOY` option (see TestPlugin below).
  - `plugins/example`: `CMakeLists.txt` → `TIER SDK`; manifest → `tier: "sdk"`, `hostBuildId: null`; includes → canonical `<QGCPluginAPI/QGCPlugin.h>` spelling (F6); dropped the host-internal `QGCLoggingCategory.h`/`QGC_LOGGING_CATEGORY` macro (unreachable from Tier SDK) for plain `Q_LOGGING_CATEGORY(..., QtWarningMsg)` — explicit third arg to keep the same default-suppressed level a review agent caught the naive two-arg form silently dropping. `ExampleRuntimePlugin` gained an `init(QGCHostServices*)` override storing the pointer with a comment demonstrating a service lookup (D4 — no services needed yet).
  - **F6** (canonical include spelling): `src/PluginAPI/CMakeLists.txt` generates `${CMAKE_BINARY_DIR}/include/QGCPluginAPI` (a symlink, falling back to a real recursive `file(COPY)` — not `CREATE_LINK`'s `COPY_ON_ERROR`, see fix below) as a `PUBLIC` include dir on the `QGCPluginAPI` target, so `<QGCPluginAPI/...>` resolves identically in-tree and for any future out-of-tree SDK-zip build.
  - **F8** (signal-name freeze): `QGCReplayService`'s `currentLogTimeSecs`/`logFileStats` renamed to event-shaped `logTimeChanged`/`logDurationChanged` (matching `playbackStarted`/`replayEnded`'s naming); `beginStream()` gained a doc paragraph distinguishing it from `play()` (one-time post-bootstrap kickoff vs. resume). Only consumer, `QGCReplayServiceImpl.cc`'s connects, updated; grepped clean repo-wide (LogReplayLink's own same-named signals are a different, unrelated class and keep their names).
  - New permanent regression fixture: `test/PluginSystem/TestPlugin/` — `TestPluginFixture`, a real SDK-tier MODULE built via `qgc_add_plugin(... NO_DEPLOY)` (never touches the runtime plugin directory). `PluginLoaderGateTest` (3 cases) loads it for real through `QGCPluginLoader::inspect()`/`activate()`: metadata gate passes tier `sdk`/`apiVersion 2`/empty `hostBuildId`, contribution synthesis matches the real embedded manifest, activation returns a live `QGCPlugin*`, and a nonexistent path fails legibly.
  - `plugins/README.md` updated: tier table (`sdk` now "Working today"), manifest example, `qgc_add_plugin()` usage guidance (prefer `SDK` unless a QGC internal is genuinely needed), tier-dependent real-linkage section, troubleshooting log line. `plugins/example/README.md` notes the Tier SDK/canonical-include shape.
  - Verified: full clean build; `otool -L` on both `libExamplePlugin.dylib` and `libTestPluginFixture.dylib` show exactly `@rpath/libQGCPluginAPI.2.dylib` + `QtCore` (+ a few transitive system frameworks) — no `dynamic_lookup`, no QtQml/Quick/Widgets/Network; live offscreen boot reached full UI construction with both the new SDK-tier Example and still-Internal-tier QDrive manifests read correctly (Qt's own loader trace showed `"IID": "org.qgroundcontrol.QGCPluginAPI/2.0"` for both), no crash. Full Unit suite green except the 3 known environmental keychain-timeout tests.
  - `/code-review medium` (8 finder angles): **3 confirmed findings, all fixed** — (1) `file(CREATE_LINK ... SYMBOLIC COPY_ON_ERROR)` on a directory source silently no-ops per CMake's own docs (creates the dest dir, copies nothing), which would leave every Tier SDK plugin's build broken with zero diagnostic wherever a symlink can't be created (e.g. Windows without `SeCreateSymbolicLinkPrivilege`) — fixed by checking `RESULT` and falling back to a real `file(COPY)` ourselves; (2) the `QGC_LOGGING_CATEGORY`→`Q_LOGGING_CATEGORY` swap silently changed the default log level (fixed, explicit `QtWarningMsg`); (3) `TestPlugin/CMakeLists.txt` hand-rolling the SDK-tier shape instead of reusing `qgc_add_plugin()` (fixed via the new `NO_DEPLOY` option, which also restores `plugins/README.md`'s "single entry point" claim). One **plausible, unfixed** finding: two agents split on whether `$<TARGET_FILE:TestPluginFixture>` embedded in a compile definition escapes Windows backslashes correctly — flagged for the eventual Windows CI run rather than guessed at from this macOS sandbox. Declined as out-of-scope/by-design: manifest-constant duplication across 3 files (pre-existing pattern, no templating system for a 2-plugin project); the TIER SDK/INTERNAL branch shape not fitting a future no-binary QML tier (refuted — plan's own Stage 3 design has QML packages skip `qgc_add_plugin()`/`add_library()` entirely, so this function was never going to host that tier); `PluginLoaderGateTest`'s missing-file case (real coverage of a real code path, not padding); `ExampleRuntimePlugin::_host` sitting unused (exactly the plan's prescribed minimal template).
  - Next: U2.6 (`export_dynamic` gated; Tier C formalized, D7) in a fresh chat.

- **U2.6 — `export_dynamic` gated; Tier C formalized (D7)** — DONE (2026-07-12).
  - `cmake/CustomOptions.cmake` gained a new "Plugin System Configuration" section with `option(QGC_ENABLE_INTERNAL_PLUGINS "Support internals-native (Tier C) plugins" ON)` (fork default ON) — placed after the Autopilot Plugin section, included (line 41) well before both consumers.
  - Root `CMakeLists.txt`: the `-Wl,-export_dynamic` block is now wrapped in `if(QGC_ENABLE_INTERNAL_PLUGINS)`.
  - `cmake/modules/PluginHelpers.cmake`'s `qgc_add_plugin()` `TIER INTERNAL` branch gained a `FATAL_ERROR` guard at the top when the option is off, mirroring the existing unsupported-tier-name error's phrasing (names the plugin, the fix, and the plan doc).
  - QDrive's manifest already declared `tier: "internal"` with a real `hostBuildId` check (U1.2) — this unit only gated the *linker flag* + the CMake-time guard, no manifest/validation changes.
  - Verified: reconfigured the existing Debug build with the default `ON` — `build.ninja`'s link line for the `QGroundControl` target still carries `-Wl,-export_dynamic`; full build succeeded, both `libExamplePlugin.dylib` (tier `sdk`) and `libQDrivePlugin.dylib` (tier `internal`) built; offscreen boot log shows both discovered with correct metadata (`hostBuildId` `a217f6e0c` matching HEAD). Fresh configure with `-DQGC_ENABLE_INTERNAL_PLUGINS=OFF`: Example (Tier SDK) configures and deploys normally; QDrive's `qgc_add_plugin(TIER INTERNAL ...)` call hits the new `FATAL_ERROR` with the expected message, halting configure — the intended "errors helpfully" behavior for this repo where QDrive is actually present (upstream, which never carries `plugins/qdrive`, would configure clean with Example only under `OFF`).
  - `/code-review low`: no findings (three small, mechanical CMake hunks).
  - Next: U2.7 (SDK packaging — the "never build QGC" artifact) in a fresh chat. **Stage 2 nearly complete.**

- **U2.7 — SDK packaging (the "never build QGC" artifact)** — DONE (2026-07-12). **Stage 2 complete.**
  - New `cmake/install/InstallPluginSDK.cmake` (included from root `CMakeLists.txt` right after `include(Install)`): one component, `QGCPluginSDK`, isolated from the app-bundle/DMG install (CMake only runs `COMPONENT`-tagged rules matching `--component`, so `cmake --install . --component QGCPluginSDK` skips every unspecified-component rule — verified empirically, see below). Installs `QGCPluginAPI` via `install(TARGETS ... EXPORT QGCPluginAPITargets)`, the 9 public headers (resolved to absolute paths from the CACHE-propagated `QGC_PLUGIN_API_PUBLIC_HEADERS` list — relative names would've resolved against the wrong directory context, caught by the first local install rehearsal), a `configure_package_config_file`-generated `QGCPluginAPIConfig.cmake` + `write_basic_package_version_file`'s `QGCPluginAPIConfigVersion.cmake` (`SameMajorVersion`, version read live off the target's own `VERSION` property rather than adding a third copy of the SDK major), `plugins/template/`, and `plugins/template/SDK-README.md` (installed to the package root). Also writes `qgc-plugin-sdk-version.txt` (the app version) so CI names the zip without re-deriving `git describe`.
  - `src/PluginAPI/CMakeLists.txt`: extracted the header list into `QGC_PLUGIN_API_PUBLIC_HEADERS` (`CACHE INTERNAL`, the same propagation pattern the root `CMakeLists.txt` already uses for `QGC_PLUGIN_INCLUDE_DIRECTORIES` — needed because `InstallPluginSDK.cmake` runs in the root directory scope, past `add_subdirectory()`'s one-way scoping) and split `target_include_directories()` into `$<BUILD_INTERFACE:...>`/`$<INSTALL_INTERFACE:include>` — required for `install(EXPORT ...)` to succeed at all; CMake fatal-errors on an exported target whose `INTERFACE_INCLUDE_DIRECTORIES` still names a bare build-tree path.
  - New `plugins/template/`: a **standalone** CMake project (`find_package(QGCPluginAPI 2 REQUIRED)`, never `qgc_add_plugin()` — that helper isn't part of the published package) with `CMakeLists.txt`, `qgcplugin.json.in` (`apiVersion` read from `QGCPluginAPI_VERSION_MAJOR`, the variable `find_package()` itself sets, instead of a fourth hardcoded copy), `MyPlugin.h/.cc` (minimal factory + runtime-plugin stub, no QML/resources — proves the bare link+load path S2m already validated), and `SDK-README.md` (compatibility contract quoted from plan-doc 02 §5.3, the ABI rules, F9's tier-scoped `apiVersion` note, a macOS signing paragraph, and the Windows/Android forward-look facts from the whole-stage ABI review). **Landmine found and fixed during grounding:** `plugins/CMakeLists.txt` auto-`add_subdirectory()`s every directory under `plugins/` with a `CMakeLists.txt` — which would have swept `plugins/template` into the in-tree build and failed its `find_package(QGCPluginAPI)` (nothing installed yet, mid-build). Fixed with a `PROJECT_IS_TOP_LEVEL` guard (false only when pulled in via that glob, true for any real standalone build) at the top of `plugins/template/CMakeLists.txt` that `return()`s immediately, rather than touching the generic discovery mechanism.
  - **F9** (tier-dependent `apiVersion`, owned by this unit per the ABI review): `PluginManifest::fromJson` now parses `apiVersion` as required+numeric only for `sdk`/`internal`; `Tier::Qml` accepts it optional (defaults to 0) and `validateForHost` skips the equality check entirely for that tier — Stage 3's binary-less QML packages never touch the C++ ABI (D1), so gating them on it was spurious. New `PluginManifestTest::_qmlTierApiVersionOptionalAndUnchecked_test`.
  - **Platform forward-look** (also owned by this unit per the tracker note): plan §10 gained the Windows soname/MSVC-CRT/`pluginInterfaceVersion()` facts and the Android W^X "Tier B from a user dir is impossible by OS policy" fact from the whole-stage ABI review; the plugin-author-facing versions live in `SDK-README.md`.
  - `plugins/README.md` gained an "SDK Package (out-of-tree Tier B)" section pointing at `plugins/template/` and its `SDK-README.md`.
  - `.github/workflows/macos.yml`: two new steps after the existing DMG "Attest and Upload" (both `if: matrix.build_type == 'Release'`) — "Package plugin SDK" runs a **second, isolated** `cmake --install . --component QGCPluginSDK --prefix qgc-plugin-sdk-macos-<version>` (reading the version from the file above) then `ditto -c -k --sequesterRsrc --keepParent` into a zip; "Attest and Upload plugin SDK" reuses the existing `attest-and-upload` composite action (SBOM + GitHub artifact + AWS) under a new `QGCPluginSDK-macos` package name.
  - Verified end-to-end, locally, standing in for CI: (1) `cmake --install <build-dir> --component QGCPluginSDK --prefix <dir>` produced exactly headers/lib/cmake-config/template/README, nothing from the app bundle; (2) `ditto`'d the result into a zip and unzipped it fresh — same layout; (3) **the real out-of-tree rehearsal**: `cmake -S template -B build -DCMAKE_PREFIX_PATH=<installed-SDK>` (only the SDK + stock Qt on the prefix path, no QGC source anywhere) configured and built `libMyPlugin.dylib` cleanly; `otool -L` showed exactly `@rpath/libQGCPluginAPI.2.dylib` + `QtCore` + system frameworks, no `dynamic_lookup`; manifest's `apiVersion` came out `2`, sourced from `find_package()`, not hardcoded; (4) dropped that dylib into the real dev QGC's user plugins directory and ran the actual app offscreen (`QT_QPA_PLATFORM=offscreen`) — log showed `MyPlugin initialized with host services`, i.e. the manifest gate passed and `init(host)` received a live host, then removed the test dylib to leave the dev plugins directory as U2.6 left it (Example + QDrive only). Full app build green; `PluginManifestTest`/`QGCPluginManagerTest`/`PluginContributionsTest`/`HostServicesTest`/`PluginLoaderGateTest` all green (20/14/11/8/3 → manifest test now +1 for the qml case).
  - `/code-review low`: 1 finding, fixed — `install(TARGETS QGCPluginAPI ... INCLUDES DESTINATION include)` duplicated the `$<INSTALL_INTERFACE:include>` entry `src/PluginAPI/CMakeLists.txt` already sets on the same target's `INTERFACE_INCLUDE_DIRECTORIES`; removed `INCLUDES DESTINATION` from `InstallPluginSDK.cmake`'s `install(TARGETS ...)` (re-verified: exactly one entry in the generated `QGCPluginAPITargets.cmake`).
  - What definition-of-done #1 still needs beyond this unit: a rehearsal against a QGC host built at a genuinely *different commit* (this session's rehearsal used the same build's host, just a completely separate install/build tree for the SDK+plugin side) — Stage 5's golden-plugin CI job is where that becomes a permanent, automated check.
  - Next: Stage 3, U3.1 (package discovery + Tier A synthesis) in a fresh chat.

### Stage 3 — Packages, install UX, trust — entered as `/architecture-change` (per Stage 2's carryover)

- **U3.1 — Package discovery + Tier A synthesis** — DONE (2026-07-13).
  - `QGCPluginLoader` gained package awareness (D8): `PluginLoadInfo` grew `packageDir` (absolute path to the package directory; empty for a bare dev-loop dylib). New `inspectPackage(packageDir)` reads `qgcplugin.json` **directly** (`QFile`/`QJsonDocument`, not `QPluginLoader::metaData()`) — a package's identity/contributions always come from this sidecar file, never a binary's own embedded metadata. `inspectDirectories()` now also scans each search root's child directories for one containing `qgcplugin.json` and routes it through `inspectPackage()`, alongside the existing bare-file scan (both discovered side by side).
  - Tier `qml` (Tier A): no `bin/` at all — `activate()` is a no-op for this tier (nothing to instantiate; `info.plugin` stays null), contributions synthesize from the manifest alone. Tier `sdk`: binary resolved via new `findPackageBinaries()` — documented key `bin/macos-universal/<name>.dylib` first, falling back to any single binary under `bin/macos-*/`; missing/multiple candidates fail legibly.
  - `PluginContributions::fromManifest` gained the `packageDir` parameter deferred from U2.2: new `resolveUrl()` implements the URL rule — `qrc:/…`, bare `/…`, and any URL already naming a scheme (`scheme://…`) pass through verbatim; anything else resolves to `file://<packageDir>/<url>` for a package, or passes through unresolved for a bare dylib (no package context to resolve against). All 14 existing call sites updated (production + tests) to pass an explicit `packageDir` (`QString()` for the non-package path) rather than defaulting it, matching the codebase's preference for explicit over implicit.
  - `PluginManifest` gained `qmlApiVersion` (F9's QML API level, 02 §5.4): meaningful — and therefore parsed/validated — only for tier `qml`; ignored on every other tier so a stray/malformed value there can't break an otherwise-valid manifest. New host constant `QGCPluginQmlApiLevel = 1` (`PluginManifest.h`), threaded into `HostInfo`/`hostInfo()`. Declared values are checked against the host's; undeclared (or explicit `0`, rejected outright — `0` is never a valid host level) skips the check.
  - `QGCPluginManager::_activateRecord` gained a null-plugin guard around `init(host)`/replay-extension query (Tier A has neither); `reloadPlugin()` now dispatches to `inspect()` or `inspectPackage()` based on `packageDir`, so a package re-inspects via its directory (sidecar-governed) rather than misreading a stale binary path.
  - **Design decision made during grounding (not in the original plan text):** `inspectPackage()` rejects tier `internal` outright — packages are `sdk`/`qml` only. Reasoning: tier `internal`'s whole trust model is a `hostBuildId` match on the *file that was inspected*; for a package, the sidecar manifest (which carries the `hostBuildId` gate) and the `bin/` binary (the code that actually runs) are two independent artifacts, and nothing re-validates the binary's own embedded metadata against the sidecar after the gate passes. Packaging tier `internal` would let a package's binary be swapped post-gate without the loader noticing — a real trust-boundary gap a review agent surfaced. Tier `internal` stays dev-loop/bare-dylib only (D7's framing already pointed this direction: "macOS/Linux-only," QDrive's actual usage).
  - Tests: `PluginLoaderGateTest` grew from 3 to 10 cases — Tier A package activates without a binary (contributions resolved package-relative), Tier B package resolves and loads a real binary through the documented key, bad-layout errors (no bin/, empty bin/, ambiguous binaries) are legible, a qml-tier package that ships a binary or declares `replay`/`telemetryLogging` is rejected, `inspectDirectories()` discovers a package alongside a bare dylib, and a tier-`internal` package is rejected. `PluginManifestTest` grew to 24 (qmlApiVersion checked/undeclared/tier-ignored/zero-rejected). `PluginContributionsTest` grew to 12 (package-relative resolution, qrc:/host-resource/absolute-scheme pass-through, no-package-context pass-through).
  - Verified: full app build clean; full Unit suite green except the 3 known environmental keychain-timeout tests; offscreen boot reaches full UI construction with no crash (loader changes are load-bearing on startup — a broken guard would surface immediately per S3's negative-control precedent).
  - `/code-review high` (8 finder angles + 8 verifiers, ~20 agent spawns): **8 confirmed findings, 6 fixed** — tier `internal` packages unable to be identity-verified against their binary (the design decision above); a bare `.dylib` mistakenly declaring `tier: "qml"` silently never ran its code (activate() no-op with zero diagnostic) → `inspect()` now rejects tier `qml` outright, mirroring `inspectPackage()`'s "qml must have no binary" check; `resolveUrl()` mangled absolute URLs naming another scheme (`http://`, `file://`) into nonsense `file://` URLs → scheme-aware pass-through added; `qmlApiVersion`'s type check wasn't tier-gated (a malformed value broke an unrelated sdk/internal manifest) → gated to tier `qml` only, combined with rejecting an explicit `0`; the "multiple candidate binaries" error always blamed the fallback glob even when the conflict was inside the single documented directory → now lists the actual conflicting paths instead of asserting a location. **2 findings deferred** (both cleanup/reuse, not correctness, per the verifying agents' own recommendation to fix in a dedicated follow-up rather than bundle into this diff): `inspect()`/`inspectPackage()` duplicate ~23 lines of id-check/validateForHost/contributions/logging control flow with no shared helper; `inspectPackage()`'s hand-rolled JSON-file read could reuse `JsonParsing::isJsonFile()`, though that helper's implicit compressed-data sniffing on arbitrary bytes is behavior a plain manifest reader shouldn't inherit without a deliberate look. One space-in-path URL concern (Application Support directory) was investigated and **refuted** — empirically verified `QUrl`'s tolerant-mode parsing round-trips an unencoded space correctly; one `.bundle`-directory-plugin gap in the binary scan was refuted **as a regression** — the `QDir::Files`-excludes-directories pattern predates this diff (inherited from the existing bare-file scan) and is dead code in practice (`qgc_add_plugin()` never produces a directory-style bundle on macOS).
  - Next: U3.2 (install/remove UX) in a fresh chat.
